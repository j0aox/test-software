export default class Product {
  constructor(dedscription, buyPrice, sellPrice, stock) {
    this.dedscription = dedscription
    this.buyPrice = buyPrice
    this.sellPrice = sellPrice
    this.stock = stock
  }
}

//module.exports = Product